<?php

/**
 * PHPStan-only stub: association-manager.php defines these via
 * plugin_dir_path()/plugin_dir_url() calls that PHPStan can't statically
 * resolve to a value, so it doesn't register the constants for use
 * across other analysed files. This stub exists purely so PHPStan knows
 * the constants exist and are strings - it is never loaded at runtime.
 */

define( 'AM_PLUGIN_FILE', __DIR__ . '/association-manager.php' );
define( 'AM_PLUGIN_DIR', __DIR__ . '/' );
define( 'AM_PLUGIN_URL', 'https://example.test/wp-content/plugins/association-manager/' );
define( 'AM_PLUGIN_VERSION', '0.3.0-alpha.1' );
